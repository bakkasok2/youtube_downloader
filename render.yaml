services:
  - type: web
    name: jini-youtube-downloader
    runtime: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: MAX_DOWNLOAD_MB
        value: "500"
      - key: YOUTUBE_PROXY_URL
        sync: false
